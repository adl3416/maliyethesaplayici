/**
 * iOS-specific configuration
 * Override or extend shared config for iOS only
 */

export const iosConfig = {
  ios: {
    supportsTablet: true,
    // iOS-specific settings can be added here
  },
};
